// Evaluate the expression "(2 + ( 3 + (5 * 3) ) )" using stack
#include "lib/stack.h"

int main()
{
  char expr="(2 + ( 3 + (5 * 3) ) )";
  stack_st o_s; // Operation Stack
  stack_st d_s; // Data Stack
}
